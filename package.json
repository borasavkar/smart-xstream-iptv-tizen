{
  "name": "smart-xtream-tizen",
  "version": "0.1.0",
  "private": true,
  "description": "Smart Xtream AI IPTV Player for Samsung Tizen TV (web app)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node build.mjs",
    "build:fast": "node build.mjs",
    "icon": "node scripts/make-icon.mjs",
    "serve": "node serve.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
